input Sort {
  updatedAt: String
  createdAt: String
  recordedAt: String
  _id: String
}

input Option {
  sort: Sort
  limit: Int
  skip: Int!
}

input Condition {
  isDeleted: Boolean
  isBlocked: Boolean
  type: String
}