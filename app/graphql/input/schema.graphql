input Sort {
  updatedAt: String
  createdAt: String
  recordedAt: String
  _id: String
}

input Option {
  sort: Sort
  limit: Int
  skip: Int!
}

input SpeechRequest{
   queryText: String
   userId: String
}

input UserInput {
  name: String!
  password: String
  description: String
  wechat: String
  phone: String
  img: String
  sex: String
  #确认密码
  ensurePassword: String
}

type result {
  status: String!
  msg: String
  data: User
}

input Condition {
  isDeleted: Boolean
  isBlocked: Boolean
  type: String
}