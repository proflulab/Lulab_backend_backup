type Query {
  #验证码相关
  # sms
  verifySend(mobile: String!, area: Int!): Res!
  verifyCheck(mobile: String!, code: String!, area: Int!): Res!
  # user
  userInfo: User!
  loginCaptcha(mobile: String!, area: Int!, code: String!): Return!
  #loginOne_click(mobile: String!, accessCode: String!, outId: String!): Return!
  loginPassword(mobile: String!, area: Int!, password: String!): Return!
  logOut: Res!
  #课程请求
  courseCategory: [Category!]
  course(category_id: String!, page: Int, limit: Int): [CourseDetail!]
  courseCatalogue(course_id: String!): [Catalogue!]
  courseLink(detail_id: String!): Link!
  
  #stripe paymentIntent支付
  createCheckoutSession:sessionId
  createPaymentIntent(items:Items!):clientSecret!
  retrievePaymentIntent(id:String!):paymentIntentStatus!
  searchPaymentIntent:[paymentIntentInfo]
  #stripe refunds支付
  createRefunds(id:String!):paymentIntentStatus!

  # 获取支付信息
  reToken(token: String!): Token!

  # user
  user: [User!]

  # 获取支付信息
  orderInfo(number: Int!): OrderInfo!

  # 七牛云
  qiniu: Qiniu!

  #退出登陆

  #获取商品信息
  goodInfo(number: Int!): GoodRes!
}
