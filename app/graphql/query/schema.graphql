type Query {
  
  #验证码相关
  verifySend(mobile: String!, area: Int!): Res!

  #verifyCheck(mobile: String!, code: String!): Res!

  loginCaptcha(mobile: String!, area: Int!,code: Int!): Return!
  loginOne_click(mobile: String!, accessCode: String!, outId: String!): Return!
  loginPassword(mobile: String!, password: String!): Return!
  #登陆注册
  logOut: Res!

  # user
  user: [User!]

  # 获取支付信息
  orderInfo(number: Int!): OrderInfo!

  # 七牛云
  qiniu: Qiniu!

  #退出登陆

  #获取商品信息
  goodInfo(number: Int!): GoodRes!
}
