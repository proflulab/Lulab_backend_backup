type Query {
  # user
  user: [User!]
  # orderInfo
  orderInfo: OrderInfo!

  # qiniu
  qiniu: Qiniu!

  #login(登陆接口)
  login(mobile: String!, password: String!): Return!

  #发送验证码
  sendcode(mobile: String!): Res!

  #校验验证码
  checkcode(mobile: String!,code: String!): Res!
}