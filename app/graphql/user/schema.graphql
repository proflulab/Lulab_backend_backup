type UserCredential {
	clientType: String
	openId: String
	unionId: String
	accessToken: String
	sessionKey: String # wechat only
}

enum OnboardingStatus {
	DEFAULT
	VERIFIED
	ONBOARDED
}


type User {
    _id: ID
    name: String!
    password: String
    description: String
    wechat: String
    phone: String
    sex: String
    img: String
}

input UserLoginPayload {
  jscode: String
}