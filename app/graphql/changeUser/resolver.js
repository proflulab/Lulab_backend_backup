// const { getUserData } = require('./connector');

// const resolvers = {
//     Mutation: {
//         user: (parent, args, context, info) => {
//             // 在真实情况下，这里可能有一些验证逻辑
//             // 由于这是mock测试，我们直接调用connector来获取用户数据
//             return getUserData();
//         },
//     },
// };

// module.exports = resolvers;
